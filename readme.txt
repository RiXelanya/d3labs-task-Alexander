How to run

First install it by running

yarn install --frozen-lockfile

and then run it with 

node .\index.js 1736145306 > log.txt 2> error.txt

You can replace the number with an epoch time that you want but the epoch time must be in seconds instead of miliseconds